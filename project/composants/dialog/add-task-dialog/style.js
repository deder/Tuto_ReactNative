
import { APP_COLORS } from './../../../styles/color';

export const styleLight = {
    buttonChangeStatus: {
        text:{
            color: APP_COLORS.primaryAction,   
        }
    },
    buttonDelete:{
        text:{
            color: 'red'
        }
    }
}